@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent

<root>
	<FloatingPanel Target=@Target ConfineToScreen=@true ConfinementMargin=@(new Vector2( 0.05f, 0.03f ) )>
        <Body>
            <p>@(Name ?? "null") @GetDistanceText()</p>
        </Body>
    </FloatingPanel>
</root>

@code
{

}
