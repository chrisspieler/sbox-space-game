﻿@using System.Linq;
@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@attribute [StyleSheet]

<root>
    @foreach ( var glyph in Glyphs.Values.OrderBy( v => v.ActionName ) )
    {
        <InputGlyphControl GlyphData=@glyph/>
    }
</root>
